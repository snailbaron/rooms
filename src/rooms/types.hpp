#pragma once

#include "geometry.hpp"

using Vector = geometry::Vector<float>;
using Point = geometry::Point<float>;
using Matrix = geometry::Matrix<float>;

using ScreenOffset = geometry::Vector<int>;
using ScreenPoint = geometry::Point<int>;
